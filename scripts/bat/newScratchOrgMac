 #!/bin/bash

# Oppretter scratch org
sf org create scratch --definition-file config/project-scratch-def.json --alias $1 --duration-days $2 --set-default --json --wait 30

# Installer crm-platform-base ver. 0.308
sf package install --package 04tQC000001UGhtYAG --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-thread-view ver. 0.5
sf package install --package 04tKB000000Y8nqYAC --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-journal-utilities ver. 0.43
sf package install --package 04tKB000000Y9WtYAK --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-platform-reporting ver. 0.39
sf package install --package 04tKB000000Y5GEYA0 --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installer crm-platform-access-control ver. 0.178
sf package install --package 04tQC000001WWfJYAW --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-shared-flowComponents ver. 0.4
sf package install --package 04t7U0000008qz4QAA --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-henvendelse-base ver. 0.38
sf package install --package 04tQC000001So7lYAC --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-platform-integration ver. 0.173
sf package install --package 04tQC000001Ms4PYAS --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-shared-user-notification ver. 0.25 
sf package install --package 04tQC000000tKMHYA2 --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-platform-oppgave ver. 0.84
sf package install --package 04tQC000001UvwLYAS --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-community-base ver. 0.121
sf package install --package 04tQC000000ieEfYAI --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-henvendelse ver. 0.207 
sf package install --package 04tQC000001TjKXYA0 --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-shared-timeline ver. 1.29
sf package install --package 04tKB000000Y8znYAC --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-sf-saf ver. 0.38
sf package install --package 04tQC000000zUijYAE --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Installerer crm-hot-felles ver. 0.19
sf package install --package 04tQC000001XkjRYAS --no-prompt --installation-key $3 --wait 30 --publish-wait 30

# Dytt kildekoden til scratch org'en
sf project deploy start

# Tildeler permissionset
sf org assign permset --name HOT_Servicetjenesten_Admin

# Opprett testdata
sf apex run --file scripts/apex/createTestData.apex

# Importerer testdata for STO
sf data import tree --plan dummy-data/sto/data/plan.json
sf data import tree --plan dummy-data/sto/plan.json 
# Importerer samtalenotater
sf data import tree --plan dummy-data/conversation_notes/plan.json 

# Oppretter testdata for STO 
sf apex run --file dummy-data/GenerateData.apex

# done